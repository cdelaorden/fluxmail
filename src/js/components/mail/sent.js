/**
 * @jsx React.DOM
 */

var React = require('react');

var SentView = React.createClass({

  render: function() {
    return (
      <div>Sent mails</div>
    );
  }

});

module.exports = SentView;